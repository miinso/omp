# no targets — consumers use the omp extension directly:
#   omp = use_extension("@omp//:extensions.bzl", "omp")
#   use_repo(omp, "libomp")
# then: deps = ["@libomp//:omp"], copts = OMP_COPTS (from @omp//:defs.bzl)
